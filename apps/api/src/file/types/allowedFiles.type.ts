export enum AllowedFileTypes {
  video = 'video',
  image = 'image',
}

export enum SubFolderName {
  video = 'videos',
  image = 'images',
  thumbnail = 'thumbnails',
}

export const allowedFileTypes = Object.keys(AllowedFileTypes);
